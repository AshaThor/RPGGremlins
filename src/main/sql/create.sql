CREATE TABLE
	character
	(
		id INTEGER NOT NULL,
		name varchar NOT NULL,
		char_class varchar NOT NULL,
		race varchar NOT NULL,
		level INTEGER,
		player varchar NOT NULL
	);
CREATE TABLE
	hibernate_sequence
	(
		next_val INTEGER
	);
